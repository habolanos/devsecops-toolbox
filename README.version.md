# 📋 Historial de Versiones

> **Único punto de verdad para el versionado del proyecto.**
> Las fechas y versiones aquí documentadas se aplican a todo el toolbox (GCP, AZDO, AWS, Terminal).

---

## Versión Actual

**`1.7.51`** — 2026-08-31

---

## Registro de Cambios

| Fecha | Versión | Descripción | Archivos / Scope |
|-------|---------|-------------|----------------|
| 2026-08-31 | **1.7.51** | **feat(aws): Homologación de GCP Cloud Run Monitoring a AWS ECS Fargate**: Tool 41 `aws_ecs_fargate_metrics_monitor.py` muestra requests, latencia p95 (ms), CPU%, memoria% y error rate de servicios ECS Fargate. Módulo base `cloudwatch/aws_cloudwatch_metrics.py` con `get_ecs_fargate_usage_metrics` (CloudWatch) y `get_ecs_fargate_metrics_parallel` (paralelo). Registrado en `aws/tools.py` grupo monitoring. 17 tests unitarios nuevos (12 para métricas, 5 para monitor). | `scm/aws/cloudwatch/aws_cloudwatch_metrics.py`, `scm/aws/cloudwatch/aws_ecs_fargate_metrics_monitor.py`, `scm/aws/tools.py`, `scm/aws/tests/test_aws_cloudwatch_metrics.py`, `scm/aws/tests/test_aws_ecs_fargate_monitor.py`, `scm/aws/README.md`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-31 | **1.7.50** | **Fix main.py: Unificar platform_map en todas las funciones**: Las 4 funciones (`get_platform_config`, `is_platform_configured`, `validate_platform_authentication`, `prepare_env_for_platform`) tenian `platform_map` inconsistentes entre si y con el dict `PLATFORMS` (fuente de verdad). Unificadas a: 1=gcp, 2=azure, 3=aws, 4=azdo, 5=terminal, 6=kpi_analyzer. Eliminada rama muerta `dashboard` en `prepare_env_for_platform`. Tests actualizados: AZDO usa key "4" (antes "2"). 6 tests fallando en CI ahora pasan (57/57). | `scm/main.py`, `scm/tests/unit/test_main.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-28 | **1.7.49** | **Fix Tool 43 Pipeline CD Clone: Auto-replace agent pools en error 403, pool fallback 5331**: (1) **extract_agent_pools**: Nueva funcion que extrae los agent pools referenciados en environments (queueId) y deployPhases (phaseInput.queueId), con dedup. (2) **replace_agent_pools**: Reemplaza todos los queueId por un pool alternativo (default 5331), remueve el objeto `queue` asociado. (3) **Error 403 auto-retry**: Cuando el POST falla con 403, lista los agent pools referenciados, reemplaza por pool 5331 y reintenta automaticamente. Si el reintento tambien falla, muestra mensaje con posibles causas. (4) **Tests**: 12 tests nuevos (7 extract_agent_pools, 5 replace_agent_pools). 119 tests total en verde. | `scm/azdo/pipeline_cd_clone/pipeline_cd_clone.py`, `scm/azdo/pipeline_cd_clone/test_pipeline_cd_clone.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-28 | **1.7.48** | **Fix Tool 43 Pipeline CD Clone: Error handling 403, modo interactivo desde tools.py, _parse_error_body**: (1) **Fix tools.py**: Tool 43 ahora pasa `--interactive` al script (igual que Tool 27), evitando el error de `--source-id` faltante. (2) **_parse_error_body**: Nueva funcion que extrae el mensaje de error de respuestas HTTP de Azure DevOps (parsea JSON, busca `message` o `innerException.message`). (3) **api_get/api_post**: Ahora capturan HTTPError, parsean el body y relanzan con el mensaje extraido. (4) **Error 403 en POST**: Mensaje especifico con posibles causas (permisos Create, permisos Use sobre agent pools, contactar administrador). (5) **Error handling en modo interactivo**: GET ahora tiene try/except con manejo de 404. (6) **Validacion source_id**: Modo interactivo valida que el ID sea numerico. (7) **Tests**: 5 tests nuevos para `_parse_error_body` (json con message, innerException, invalid json, empty, sin message). 107 tests total en verde. | `scm/azdo/tools.py`, `scm/azdo/pipeline_cd_clone/pipeline_cd_clone.py`, `scm/azdo/pipeline_cd_clone/test_pipeline_cd_clone.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-28 | **1.7.47** | **Test: Tests unitarios para tools.py (menu, TOOLS, config, helpers)**: 78 tests unitarios cubriendo `scm/azdo/tools.py`. (1) **Colors**: Atributos definidos, aliases RED=FAIL, YELLOW=WARNING. (2) **TOOL_GROUPS**: Campos obligatorios (name, emoji, color), consistencia con GROUP_ORDER, grupo system existe. (3) **TOOLS**: Campos obligatorios (name, description, group, status), grupos validos, statuses validos, paths son strings, args son lists. (4) **Tools especificos**: Tool 43, 42, 41, 27 existen con nombre y grupo correcto. (5) **System options**: A, B, Q construidas con auto_tools y exit status. (6) **STATUS_INDICATORS**: 3 elementos por status, statuses esperados presentes. (7) **_menu_sort_key**: Numeric, alpha suffix, pure alpha, sorting order. (8) **get_auto_tools**: Exclusiones, system keys excluidas, orden por grupo. (9) **get_menu_order**: Q al final, todas las keys validas. (10) **build_system_options**: A/B/Q con auto_tools y exit. (11) **config_get**: Simple, nested, missing, None, non-dict, deeply nested. (12) **load_config**: No file, valid, corrupt. (13) **save/load_last_params**: Roundtrip, missing, multiple tools. (14) **_req_hash**: Consistent, different content, nonexistent. (15) **prompt**: Default, user input, empty default. (16) **log_command**: Env var control. (17) **run_tool**: Invalid key, Q exits. (18) **_JSON_FORMAT_TOOLS/_CACHE_JSON_TOOLS**: Sets, subset of TOOLS. (19) **_print_execution_summary**: All OK, with errors, empty. (20) **Script paths**: All ready tools have existing scripts. (21) **Args consistency**: defaults subset of args. | `scm/azdo/test_tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-28 | **1.7.46** | **Feat Tool 43 Pipeline CD Clone: Clonar pipeline CD completo desde definition ID**: Nueva herramienta que clona un Pipeline CD completo a partir de un definition ID existente. (1) **Nombre default**: Pregunta nombre del nuevo pipeline, default `LAB-{nombre_actual}` si se presiona Enter. (2) **Path default**: Path del nuevo pipeline por defecto es el mismo que el origen, con opcion de cambiarlo. (3) **Limpieza**: Remueve campos del sistema (`id`, `revision`, `createdBy`, `modifiedBy`, `_links`, etc.) y campos de environments (`id`, `releaseId`). (4) **Secrets**: Detecta variables secretas y alerta que los valores no se copian. (5) **Backup**: Crea backup JSON del pipeline origen antes de clonar. (6) **Dry-run**: Modo simulacion que muestra que se crearia sin hacer el POST. (7) **Rich UI**: Spinner, barras de progreso, tablas Rich y paneles para info del pipeline. (8) **Modo interactivo**: Org/proyecto/PAT informativos de config.json, pregunta source ID, nombre y path. (9) **Reporte**: Exporta reporte JSON con trazabilidad source→target. (10) **Tests**: 24 tests unitarios (normalize_org, auth header, clean_definition, extract_secrets, build_clone_payload, backup, source_id_check, colors). | `scm/azdo/pipeline_cd_clone/pipeline_cd_clone.py`, `scm/azdo/pipeline_cd_clone/__init__.py`, `scm/azdo/pipeline_cd_clone/test_pipeline_cd_clone.py`, `scm/azdo/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.45** | **Fix Tool 42 Release Updater: Comment del template, scope '*' skip silencioso**: (1) **Comment**: `metadata.comment` del template ahora se envia como `payload['comment']` al PUT del release (antes se ponía en `description`). El comment aparece en el historial de revisiones del release en Azure DevOps. (2) **Scope '*' skip silencioso**: Cuando una variable con scope `'*'` solo existe a nivel release (no en environments), la busqueda env_var ya no genera error — se omite silenciosamente. Solo genera error si la variable existe en algun environment pero con valor diferente al search_value. (3) **Print comment**: `load_template` ahora muestra el comment en el resumen de configuracion cargada. (4) **Tests**: 2 tests nuevos (wildcard skip, comment payload). 68 tests total en verde. | `scm/azdo/pipeline_cd_update_release/pipeline_cd_update_release.py`, `scm/azdo/pipeline_cd_update_release/test_update_release.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.44** | **Fix Tool 42 Release Updater: Renombrar scope 'global' a 'release', scope '*' (todos), buscar workflowTasks en release instances**: (1) **Renombrar scope**: `scope: 'global'` renombrado a `scope: 'release'` para coincidir con terminologia de Azure DevOps (scope Release = variables que aplican a todos los stages). `global` sigue funcionando por backward compat. (2) **Scope '*'**: Nuevo scope `'*'` que busca y actualiza la variable tanto en `release.variables` como en `environments[].variables` (todos los scopes). (3) **Fix workflowTasks**: Release instances tienen estructura diferente a definiciones: buscan `workflowTasks` en `env.deployPhases[]`, `env.workflowTasks[]` directo, y `env.deployPhasesSnapshot[]`. Antes solo buscaba en `deployPhases` que no existe en release instances. (4) **Debug logging**: Warning si environment no tiene `workflowTasks` en ninguna ubicacion. (5) **Template**: `release_update_branchconfig.yaml` v1.3 migrado a `scope: 'release'`, agrega `k8ManifestPath`. (6) **Tests**: 2 tests nuevos (release scope, wildcard scope, global backward compat). 66 tests total en verde. | `scm/azdo/pipeline_cd_update_release/pipeline_cd_update_release.py`, `scm/azdo/pipeline_cd_update_release/test_update_release.py`, `scm/templates/release_update_branchconfig.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.43** | **Fix Tool 42 Release Updater: Dry-run prompt, global scope search, Colors.DIM, template selection**: (1) **Fix dry-run**: Template `options.dry_run` ya no fuerza dry-run sobre la eleccion del usuario. `tools.py` ahora pregunta explicitamente por dry-run en opcion 1. Todos los templates cambiados a `dry_run: false`. (2) **Fix global scope search**: `search.variables` ahora soporta `scope: global` para buscar variables en `release.variables` (globales) en lugar de `environments[].variables`. `load_template` mueve variables con scope global a `global_vars` con `global_var_search_values`. `build_patch_payload` filtra global vars por search_value. (3) **Fix Colors.DIM**: Agregado `DIM` a clase `Colors` en `tools.py` (faltante, causaba error en modo interactivo). (4) **Fix template selection**: Submenu interactivo ahora pregunta si usar template (default S), lista templates disponibles, usa rutas relativas (`scm/templates/...`), org/proyecto/PAT son informativos (precargados de config.json). (5) **Fix template path**: `SCM_ROOT/templates` en lugar de `BASE_DIR/scm/templates`. Ruta absoluta al subprocess. (6) **PyYAML**: Agregado a `requirements.txt` e instalado en venv. (7) **Template**: `release_update_branchconfig.yaml` migrado a `scope: global` para `branchConfig`. (8) **Tests**: 3 tests nuevos (global scope load_template, global var search match, global var search no match). 64 tests total en verde. | `scm/azdo/pipeline_cd_update_release/pipeline_cd_update_release.py`, `scm/azdo/pipeline_cd_update_release/test_update_release.py`, `scm/azdo/tools.py`, `scm/azdo/requirements.txt`, `scm/templates/release_update_branchconfig.yaml`, `scm/templates/release_abandon_stale.yaml`, `scm/templates/release_update_git_credentials.yaml`, `scm/templates/release_update_node_version.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.42** | **Feat Tool 42 Release Updater: Seccion search en templates + backward compatible**: (1) **Nueva estructura search**: `load_template` ahora soporta seccion `search` con `stages` (lista de nombres o `*`), `variables` (name+value para search_value) y `release_ids` (alternativa a release.ids). (2) **Separacion de responsabilidades**: `search.stages` define en que stages aplicar, `search.variables` define el filtro de valor actual, `update.env_vars` solo tiene name+value. (3) **Task filtering**: `build_patch_payload` ahora filtra task updates por `search_stages` (si no es `*`). (4) **Backward compatible**: templates antiguos sin `search` siguen funcionando (env_vars con `stage` y `search_value` inline). (5) **Template**: `release_update_branchconfig.yaml` v1.2 migrada a nueva estructura. (6) **Tests**: 5 tests nuevos para search section (wildcard, specific stages, no variables, release_ids, backward compatible). 59 tests total en verde. | `scm/azdo/pipeline_cd_update_release/pipeline_cd_update_release.py`, `scm/azdo/pipeline_cd_update_release/test_update_release.py`, `scm/templates/release_update_branchconfig.yaml`, `VERSION`, `README.md`, `README.version.md`, `scm/templates/README.md` |
| 2026-08-27 | **1.7.41** | **Feat Tool 42 Release Updater: Task updates en releases + template unificado branchConfig**: (1) **Task updates**: `build_patch_payload` ahora soporta actualizacion de campos de tasks dentro de release environments (`deployPhases[].workflowTasks[]`). Busca task por `displayName` (case-insensitive) y reemplaza texto en el campo especificado via `path` (e.g. `inputs.script`). (2) **Template unificado**: `release_update_branchconfig.yaml` v1.1 ahora incluye ambos cambios en una sola template: actualiza variable `branchConfig` (config-cadenaSuministro -> feature/feature-amad) en todos los stages con search_value, Y reemplaza `path_pipelineConfig` por `path_pipelineConfigYml` en task "get file k8-manifest". (3) **Eliminado**: `pipe_cd_update_path_pipelineConfig.yaml` (Tool 41) — el cambio va en Tool 42. (4) **Helpers**: `_get_nested_value` y `_set_nested_value` para acceso a paths anidados en dicts. (5) **Tests**: 5 tests nuevos para task updates (replace in script, no match skip, case insensitive, task not found, combined env_vars + task_updates). 54 tests total en verde. | `scm/azdo/pipeline_cd_update_release/pipeline_cd_update_release.py`, `scm/azdo/pipeline_cd_update_release/test_update_release.py`, `scm/templates/release_update_branchconfig.yaml`, `VERSION`, `README.md`, `README.version.md`, `scm/templates/README.md` |
| 2026-08-27 | **1.7.40** | **Feat Tool 42 Release Updater: Wildcard stage y search_value + Templates branchConfig y path_pipelineConfig**: (1) **Wildcard stage**: `build_patch_payload` ahora soporta `stage: "*"` en env_vars para actualizar una variable en todos los stages del release. (2) **search_value**: Nuevo campo opcional `search_value` en env_vars del template YAML que solo actualiza la variable si el valor actual coincide. (3) **Template Tool 42**: `release_update_branchconfig.yaml` busca `branchConfig=config-cadenaSuministro` en todos los stages y actualiza a `feature/feature-amad`. (4) **Template Tool 41**: `pipe_cd_update_path_pipelineConfig.yaml` busca task "get file k8-manifest" y reemplaza `path_pipelineConfig` por `path_pipelineConfigYml` en `inputs.script`. (5) **Tests**: 7 tests nuevos (wildcard all, wildcard+search_value filter, wildcard+search_value no match, specific stage+search_value match/no match, wildcard var not found, load_template with search_value). 49 tests total en verde. | `scm/azdo/pipeline_cd_update_release/pipeline_cd_update_release.py`, `scm/azdo/pipeline_cd_update_release/test_update_release.py`, `scm/templates/release_update_branchconfig.yaml`, `scm/templates/pipe_cd_update_path_pipelineConfig.yaml`, `VERSION`, `README.md`, `README.version.md`, `scm/templates/README.md` |
| 2026-08-27 | **1.7.39** | **Feat Tool 42 Release Updater Template: Soporte para templates YAML**: Tool 42 (`pipeline_cd_update_release.py`) ahora soporta templates YAML declarativos via flag `--template`. Estructura del template: `metadata`, `release.ids`, `update.global_vars`, `update.env_vars`, `update.abandon`, `update.description`, `options.dry_run`, `options.backup_path`. Los flags CLI sobrescriben los valores del template. 3 templates creados: `release_update_git_credentials.yaml` (renovar credenciales Git), `release_update_node_version.yaml` (actualizar NODE_VERSION por environment), `release_abandon_stale.yaml` (abandonar releases obsoletos). 6 tests nuevos para `load_template` (global_vars, env_vars, abandon, empty_ids, not_found, string_ids). 42 tests total en verde. | `scm/azdo/pipeline_cd_update_release/pipeline_cd_update_release.py`, `scm/azdo/pipeline_cd_update_release/test_update_release.py`, `scm/azdo/tools.py`, `scm/templates/release_update_git_credentials.yaml`, `scm/templates/release_update_node_version.yaml`, `scm/templates/release_abandon_stale.yaml`, `scm/templates/README.md`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.38** | **Remove Tool 21 Pipeline Updater (branchConfig)**: Eliminada Tool 21 `pipeline-cd-update-branchconfig.py` por ser redundante con Tool 41 (`pipeline_updater/pipeline_updater.py`). Tool 41 cubre completamente todas las funcionalidades de Tool 21 (modificar variables, actualizar scripts de tasks) mediante templates YAML declarativos, con soporte adicional para stages, triggers, artifacts, ejecucion paralela y rollback. Cambios: (1) Removido entry `"21"` del dict TOOLS en `tools.py`. (2) Removido caso especial `if tool_key == "21"` en `run_tool`. (3) Eliminado archivo `pipeline-cd-update-branchconfig.py`. | `scm/azdo/tools.py`, `scm/azdo/pipeline-cd-update-branchconfig.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.37** | **Feat Pipeline CD Update Release: Actualizar releases por releaseId via PATCH API**: Nueva herramienta Tool 28 `pipeline_cd_update_release.py` que permite actualizar un Release existente por `releaseId` (no `definitionId`) usando `PATCH /release/releases/{releaseId}`. Soporta: (1) **Variables globales**: agregar/modificar via `--set-var NOMBRE=VALOR`. (2) **Variables por environment**: agregar/modificar via `--set-env-var STAGE,NOMBRE=VALOR` (case-insensitive). (3) **Abandonar release**: `--abandon` setea status=abandoned. (4) **Descripcion**: `--description`. Incluye backup automatico pre-update, modo dry-run, soporte multi-release (IDs separados por coma), modo interactivo, Rich tables para info del release y diff de cambios, reporte JSON con trazabilidad. 36 tests unitarios cubriendo parse_var, parse_env_var, build_patch_payload (global/env vars, abandon, description, combined, no-mutation), normalize_org, create_auth_header, create_backup, export_report, get_release (mock), update_release (mock). | `scm/azdo/pipeline_cd_update_release.py`, `scm/azdo/test_update_release.py`, `scm/azdo/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.36** | **Feat Release CD Health: Redistribucion de pesos score (60+20+20)**: Ajuste de pesos de la formula de score: Recencia 50→60 pts, Estabilidad Definicion 30→20 pts. Estabilidad Deploy se mantiene en 20 pts. La recencia ahora domina con 60% del score. Formula: `60 × (1 - dias/365)` + `20 - (intentos-1) × 7` + `min(20, 20 × dias_sin_mod/180)`. Tests actualizados. 276 tests en verde. | `scm/azdo/azdo_release_cd_health.py`, `scm/azdo/test_release_cd_health.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.35** | **Feat Release CD Health: Columnas informativas de ultimo release en reportes**: Agrega 3 columnas al HTML dashboard y export (JSON/CSV/Excel): `last_release_id`, `last_release_name`, `last_release_date`. Estas columnas muestran el ultimo release global (independiente de stage production). `analyze_releases` ahora extrae nombre y fecha del ultimo release antes del check de `prod_stage`. Tabla Rich y fallback print sin cambios. 276 tests en verde. | `scm/azdo/azdo_release_cd_health.py`, `scm/azdo/test_release_cd_health.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-27 | **1.7.34** | **Feat Release CD Health: 3-component score formula**: Nueva formula de score con 3 componentes para `azdo_release_cd_health.py`: (1) **Recencia** (0-50 pts): `50 × (1 - dias_deploy/365)` — deploy reciente = mas puntos. (2) **Estabilidad Deploy** (0-20 pts): `20 - (intentos-1) × 7` — menos intentos = mas puntos. (3) **Estabilidad Definicion** (0-30 pts): `min(30, 30 × dias_sin_modificar/180)` — mas tiempo sin cambios en la definicion = mas puntos. Extrae `modifiedOn` de la Release Definition via API. Formula anterior era 2 componentes (Recencia 70 + Estabilidad 30). 7 tests nuevos para `compute_score`. Export flat data y HTML dashboard actualizados con `score_definition`, `days_modified`, `modified_on`. 276 tests total en verde. | `scm/azdo/azdo_release_cd_health.py`, `scm/azdo/test_release_cd_health.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-12 | **1.7.33** | **Feat copy_from artifact_filters**: (1) **Feat artifact_filters**: Nuevo campo opcional `artifact_filters` en reglas `copy_from` que agrega conditions de tipo artifact al stage copiado para filtrar por branches. Soporta token `$auto:Git` para resolver automaticamente el primer Git artifact. Cada filtro especifica `artifact`, `type` (include/exclude) y `branches`. Las branches se expanden a formato Azure DevOps (`+refs/heads/{branch}`). `update_engine.py` `_add_stage` ahora llama `_apply_artifact_filters`. Preserva conditions de tipo event/environmentState. (2) **Template**: `pipe_cd_copy_stage_from_pipeline.yaml` actualizado v1.2 con ejemplo de artifact_filters para branches develop, QA, release/*. (3) **Tests**: 3 tests nuevos (artifact_filters basico, preservacion de event conditions, error sin artifact). 186 tests total en verde. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/test_copy_stage_from_pipeline.py`, `scm/templates/pipe_cd_copy_stage_from_pipeline.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-12 | **1.7.32** | **Feat copy_from trigger + make_dependents + Fix apply_updates error handling**: (1) **Feat trigger**: Nuevo campo opcional `trigger` en reglas `copy_from` que sobrescribe las `conditions[]` del stage copiado. Opciones: `after_release` (ReleaseStarted), `after_stage` (depende de reference_stage), `none` (manual). Preserva artifact filters. `update_engine.py` `_add_stage` ahora llama `_apply_trigger_override`. (2) **Feat make_dependents**: Nuevo campo opcional `make_dependents` en reglas `copy_from` que hace que stages existentes dependan del stage copiado. Reemplaza conditions de tipo environmentState/event por dependencia al nuevo stage. Preserva artifact filters. `update_engine.py` `_add_stage` ahora llama `_apply_make_dependents`. (3) **Fix apply_updates**: `parallel_executor.py` ahora verifica el return de `apply_updates()` y retorna `UpdateResult(success=False)` si falla, antes ignoraba el resultado. (4) **Template**: `pipe_cd_copy_stage_from_pipeline.yaml` actualizado v1.1 con ejemplo de trigger after_release y make_dependents. (5) **Tests**: 8 tests nuevos (trigger after_release, after_stage, none, artifact preservation, make_dependents, make_dependents artifact, invalid trigger, after_stage sin reference). 183 tests total en verde. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/test_copy_stage_from_pipeline.py`, `scm/templates/pipe_cd_copy_stage_from_pipeline.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-10 | **1.7.31** | **Feat copy_from cross-pipeline + Fix failed count + Rich prompts**: (1) **Feat copy_from**: Nueva accion `copy_from` en templates que permite copiar un stage desde otro pipeline (source_definition_id) e insertarlo en el pipeline destino. `parallel_executor.py` descarga el pipeline origen, extrae el stage, y lo convierte en `action:add` con definition embebida. `update_engine.py` `_add_stage` ahora soporta `task_updates` opcional. Template `pipe_cd_copy_stage_from_pipeline.yaml`. (2) **Fix failed count**: `parallel_executor.py` `failed` ahora cuenta `UpdateResult` con `success=False` + excepciones, antes solo contaba excepciones. (3) **Rich prompts**: `pipeline_updater.py` confirmacion y resumen migrados a Rich `console.print`. `tools.py` opcion 1 (Pipeline Updater) migrada a `Prompt.ask` y `Confirm.ask`. 175 tests total en verde (18 nuevos en `test_copy_stage_from_pipeline.py`). | `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/pipeline_updater.py`, `scm/azdo/pipeline_updater/test_copy_stage_from_pipeline.py`, `scm/azdo/tools.py`, `scm/templates/pipe_cd_copy_stage_from_pipeline.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-09 | **1.7.30** | **Fix azdo_client + Feat autosort_stages**: (1) **Fix isDisabled preservacion**: `azdo_client.py` removia `isDisabled` del body del PUT en updates normales (disable=False), causando que pipelines disabled se re-activaran al mover o actualizar. Fix: `isDisabled` nunca se remueve, solo se setea a True cuando disable=True. (2) **Feat autosort_stages**: Nueva accion `autosort_stages` en `parallel_executor.py` que ordena stages numericos alfanumericamente manteniendo fijos los stages no-numericos. Template `pipe_cd_autosort_stages.yaml` con opciones `fixed_stages`, `sort_pattern` (regex), `sort_order` (asc/desc). (3) **TemplateParser**: Nuevo metodo `get_pipeline_sort_config()`. (4) **Tests**: 23 tests nuevos en `test_autosort_stages.py` (parser, validator, executor flow, desc order, many stages, isDisabled preservation, real template). 2 tests actualizados en `test_update_release_definition.py` y `test_disable_pipeline.py` por cambio de isDisabled. 152 tests total en verde. | `scm/azdo/pipeline_updater/azdo_client.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/template_parser.py`, `scm/azdo/pipeline_updater/test_autosort_stages.py`, `scm/azdo/pipeline_updater/test_update_release_definition.py`, `scm/azdo/pipeline_updater/test_disable_pipeline.py`, `scm/templates/pipe_cd_autosort_stages.yaml`, `scm/templates/README.md`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-09 | **1.7.29** | **Feat pipeline_cd_backup_restore: UI Rich completa en modo interactivo y CLI**: Migracion completa de Colors ANSI a Rich en todo el script. (1) **interactive_mode**: Header con Panel Rich, submenu con Table dentro de Panel, prompts con Rich Prompt.ask, confirmaciones con Confirm.ask (S/N con estilo Rich). PAT enmascarado como `****` en prompts. (2) **prompt_with_default**: Usa Rich Prompt.ask con estilo bold y defaults cyan. (3) **print_backup_results / print_backup_all_results**: Resumen masivo con Panel Rich en vez de prints ANSI. (4) **print_backups_table**: Mensaje sin backups con console.print Rich. (5) **backup_all_pipelines**: Dry-run y empty con console.print Rich. (6) **CLI mode**: Todos los mensajes de error, confirmaciones y resultados migrados a console.print. (7) **KeyboardInterrupt**: Mensaje con console.print Rich. 52 tests en verde. | `scm/azdo/pipeline_cd_backup_restore.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-09 | **1.7.28** | **Fix tools.py: Tool 27 no lanzaba submenu interactivo**: `run_tool` no tenia caso especial para Tool 27, por lo que ejecutaba el script sin `--interactive` ni `--mode`, causando error "Especifique --mode o --interactive". Agregado caso especial `if tool_key == "27"` que lanza `pipeline_cd_backup_restore.py --interactive` directamente (mismo patron que Tool 21). | `scm/azdo/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-09 | **1.7.27** | **Feat pipeline_cd_backup_restore: Spinners y barras de progreso Rich en todas las operaciones**: Mejora de UX de Tool 27. (1) **backup_pipelines**: Progress bar mejorada con contador completed/total y status por-pipeline (OK/ERR + ID). (2) **backup_all_pipelines**: Spinner `console.status` durante fase de listado de pipelines. (3) **Restore (interactivo y CLI)**: Spinners para cargar backup, obtener definicion actual, comparar diff y restaurar. (4) **Create (interactivo y CLI)**: Spinners para cargar backup y crear pipeline. (5) **Diff (interactivo y CLI)**: Spinners para cargar backup, obtener definicion actual y comparar. (6) **List backups**: Spinner durante escaneo de directorio. (7) **Convert JSON→YAML**: Spinner para archivo unico, Progress bar con nombre de archivo para conversion masiva. 52 tests siguen en verde. | `scm/azdo/pipeline_cd_backup_restore.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-09 | **1.7.26** | **Feat pipeline_cd_backup_restore: Backup y Restore completo de definiciones de Pipeline CD**: Nueva herramienta Tool 27 `pipeline_cd_backup_restore.py` con submenú interactivo de 7 opciones. (1) **Backup Completo**: Descarga definición completa via API GET /release/definitions/{id}, resuelve nombres de agent pools/variable groups/task groups, detecta secret variables. Soporta IDs separados por coma (máx 500) y ejecución paralela con ThreadPoolExecutor. (2) **Backup Masivo**: Respalda todos los pipelines del proyecto con filtro opcional por carpeta/path. Genera archivo índice con resumen. (3) **Restore Definición**: Restaura via PUT /release/definitions/{id} con diff previo, re-ingreso opcional de secrets y confirmación interactiva. Modo dry-run. (4) **Crear Pipeline desde Backup**: Crea nueva definición via POST limpiando campos del sistema (id, revision, createdBy, etc.). Permite renombrar. (5) **Diff Backup vs Actual**: Compara variables, environments, tasks, artifacts, triggers y muestra tabla Rich con cambios (agregados/eliminados/modificados). (6) **Listar Backups**: Tabla de backups disponibles con metadata. (7) **Convertir JSON→YAML**: Genera YAML humanizado con secciones separadas (metadata, variables, artifacts, environments, triggers, retention, secrets, resolved_names) y comentarios. Formato de backup configurable: json, yaml o both. 52 tests unitarios cubriendo extract_secrets, clean_system_fields, humanize_yaml, diff_definitions, load_backup, backup_single_pipeline, restore_definition, create_from_backup, convert_json_to_yaml, list_backups, backup_all_pipelines. | `scm/azdo/pipeline_cd_backup_restore.py`, `scm/azdo/test_pipeline_cd_backup_restore.py`, `scm/azdo/tools.py`, `VERSION`, `README.version.md` |
| 2026-08-08 | **1.7.25** | **Feat release_cd_health: Columna Folder/Path del pipeline CD**: Agrega la columna `folder` (path de la carpeta del pipeline) a la herramienta `azdo_release_cd_health.py` (Tool 3). (1) **process_pipeline**: Captura `summary.path` de la API de release definitions y lo incluye en el resultado. Default `\` si no existe. (2) **Tabla Rich**: Nueva columna "Folder" entre Pipeline CD y Stages. (3) **Fallback print**: Columna Folder agregada al header y filas. (4) **HTML Dashboard**: Columna Folder sortable, incluida en busqueda/filtro. (5) **Export**: Campo `folder` en flat data (JSON/CSV/Excel). (6) **Tests**: 4 tests nuevos (folder capturado, folder default, folder en HTML, folder en export). 16 tests total en verde. | `scm/azdo/azdo_release_cd_health.py`, `scm/azdo/test_release_cd_health.py`, `VERSION`, `README.version.md` |
| 2026-08-08 | **1.7.24** | **Feat setup wizard: Configuracion inicial interactiva para primera ejecucion**: Wizard interactivo que se ejecuta automaticamente cuando `config.json` no existe o tiene placeholders en secciones requeridas (azdo, gcp, global). (1) **PrecheckStep**: Detecta CLIs instalados (gcloud, az, aws) y sesiones activas. Si un CLI esta instalado sin sesion, ofrece ejecutar `gcloud auth login` / `az login` / `aws configure` automaticamente con confirmacion del usuario. (2) **AzdoStep**: Pide organization name (no URL completa), project y PAT. Arma URL automaticamente (`https://dev.azure.com/{org}`). Acepta tanto nombre plano como URL completa como input. (3) **GcpStep**: Sugiere project_id desde sesion activa de gcloud. (4) **AzureStep** (opcional): Sugiere subscription desde `az account show`. (5) **AwsStep** (opcional): Profile y region. (6) **DashboardStep** (opcional): Webhook de Teams. (7) **GlobalStep**: Output_dir, debug, verbose. (8) **ConfigValidator**: Valida placeholders `<TU_*>` y campos requeridos. (9) **SetupWizard orchestrator**: Carga template, mergea config existente, ejecuta steps, limpia metadata `_info`, valida y guarda. (10) **Integracion en main.py**: `_run_setup_wizard_if_needed()` ejecuta wizard antes del menu principal solo si es necesario. (11) **Tests**: 68 tests unitarios cubriendo todos los steps, validator, wizard y casos edge. | `scm/setup/` (package completo), `scm/main.py`, `scm/README.md`, `README.md`, `README.version.md`, `VERSION` |
| 2026-08-05 | **1.7.22** | **Feat azdo_pipeline_history: Integracion de Git commits y tags en timeline**: Mejora de la herramienta `azdo_pipeline_history.py` (Tool 26). (1) **Git commits**: Nueva funcion `get_git_commits()` consulta la API `GET /git/repositories/{repoId}/commits` de Azure DevOps para obtener commits de la rama `master` (configurable con `--branch`) dentro del rango de meses especificado. (2) **Git tags**: Nueva funcion `get_git_tags()` consulta `GET /git/repositories/{repoId}/annotatedtags` y filtra por fecha. (3) **Repo ID auto-detection**: `extract_repo_id_from_artifacts()` extrae el ID del repositorio Git desde los artifacts de la definicion del pipeline (formato `sourceId={repoId}:{branchRef}`). (4) **Timeline Chart.js**: Nuevos datasets `Commits` (y=3, morado) y `Tags` (y=3.5, rosa, pointStyle rectRot) en el scatter plot temporal. Point radius dinamico segun archivos modificados. Tooltips con hash, autor, mensaje y archivos. Eje Y expandido con labels `Commits` y `Tags`. (5) **HTML tables**: Tabla de commits con filtro por texto (autor, mensaje, hash) y tabla de tags. (6) **Console output**: Tablas Rich con commits (hash, fecha, autor, mensaje, archivos) y tags (nombre, fecha, creador, mensaje). Panel de resumen actualizado con conteos. (7) **Args**: `--branch` (default: master) y `--no-commits` para desactivar la descarga. (8) **Tests**: 13 tests nuevos (54→67 total) cubriendo extract_repo_id (5), get_git_commits (2), get_git_tags (2), HTML con commits/tags (4). | `scm/azdo/azdo_pipeline_history.py`, `scm/tests/test_azdo_pipeline_history.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-04 | **1.7.21** | **Feat azdo_pipeline_history: Evolucion historica de Pipeline CD con HTML interactivo**: Nueva herramienta Tool 26 `azdo_pipeline_history.py`. (1) **Revisiones**: Descarga todas las revisiones de la definicion via API `GET /definitions/{id}/revisions` y filtra por rango de fechas (default 6 meses). (2) **Diff exacto**: Compara revisiones consecutivas descargando cada version completa via `?revision={n}`. Detecta cambios en stages (nombre, rank, approvals, agent pool, condition), variables (valor anterior vs nuevo, secrets enmascarados), tasks (enabled, alwaysRun, continueOnError) y artifacts (type, sourceId, isPrimary). (3) **HTML interactivo**: Timeline grafica con Chart.js (scatter plot temporal con revisiones y releases), tarjetas de resumen (revisiones, exitos, fallos, cambios), tabla de cambios con filtros por texto y categoria, tabla de releases con badges de estado. Dark theme GitHub-style. (4) **Releases**: Lista todos los releases en el rango con estado, fecha y usuario. (5) **Console output**: Tablas Rich con resumen, revisiones, releases y diffs detallados. (6) **Export**: JSON/CSV/Excel via ExportManager. (7) **Tests**: 47 tests unitarios cubriendo date helpers, extractores, diff logic (stages, variables, tasks, artifacts), HTML generation y HTTP helpers. | `scm/azdo/azdo_pipeline_history.py`, `scm/azdo/tools.py`, `scm/tests/test_azdo_pipeline_history.py`, `README.md`, `README.version.md` |
| 2026-08-04 | **1.7.20** | **Feat winget distribution + add_task action + skip PUT sin cambios + templates regionales**: (1) **Winget distribution**: Manifests YAML para Windows Package Manager (`winget install habolanos.devsecops-toolbox`). Ejecutable `devsecops-toolbox.exe` compilado con PyInstaller y subido como asset del GitHub Release. (2) **Rename ejecutable**: `toolbox` → `devsecops-toolbox` en `build_executables.py`, wrappers y `dist/README.md`. (3) **Fix build_executables.py**: `--buildpath` → `--workpath` (PyInstaller API correcto). (4) **Nueva accion `add_task`**: `_process_add_task_actions()` en `update_engine.py` inserta tasks nuevas en stages existentes antes/despues de una task de referencia por `displayName`. (5) **Skip PUT sin cambios**: `parallel_executor.py` omite el PUT a Azure DevOps cuando `changes_count == 0`. (6) **Fix search_variables**: Normaliza dicts a strings antes de `_matches_pattern()`. (7) **Template insert_task**: `pipe_cd_insert_task_before_show_manifest.yaml` con `filePath`. (8) **Templates regionales**: renombrado de stages. (9) **Tests**: 9 tests en `test_add_task.py`, 67 tests total en verde. | `build_executables.py`, `winget/manifests/h/habolanos/devsecops-toolbox/1.7.20/`, `winget/README.md`, `dist/README.md`, `README.md`, `.gitignore`, `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/search_engine.py`, `scm/azdo/pipeline_updater/test_add_task.py`, `scm/templates/pipe_cd_insert_task_before_show_manifest.yaml`, `VERSION`, `README.version.md` |: (1) **Nueva accion `add_task`**: `_process_add_task_actions()` en `update_engine.py` inserta tasks nuevas en stages existentes antes/despues de una task de referencia por `displayName`. Soporta filtro por stage y comodines fnmatch. (2) **Skip PUT sin cambios**: `parallel_executor.py` omite el PUT a Azure DevOps cuando `changes_count == 0`, evitando revisiones vacias. (3) **Fix search_variables**: Normaliza dicts a strings antes de `_matches_pattern()`. (4) **Template insert_task**: `pipe_cd_insert_task_before_show_manifest.yaml` con `filePath` apuntando al script `.sh`. (5) **Templates regionales**: `pipe_cd_rename_all_cedis_texcoco_to_regional_24-Texcoco.yaml` y `pipe_cd_rename_all_cedis_13-Villa_Hermosa_to_regional_13-Villa_Hermosa.yaml`. (6) **Script bash**: `azdo-agrupar-env-task-add_secrets_manifest+remove_datadog_clean_manifest.sh` para consolidar env y remover datadog. (7) **Tests**: 9 tests en `test_add_task.py`, 67 tests total en verde. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/search_engine.py`, `scm/azdo/pipeline_updater/test_add_task.py`, `scm/templates/pipe_cd_insert_task_before_show_manifest.yaml`, `scm/templates/pipe_cd_rename_all_cedis_texcoco_to_regional_24-Texcoco.yaml`, `scm/templates/pipe_cd_rename_all_cedis_13-Villa_Hermosa_to_regional_13-Villa_Hermosa.yaml`, `scm/templates/azdo-agrupar-env-task-add_secrets_manifest+remove_datadog_clean_manifest.sh`, `scm/templates/pipe_cd_update_var_branchConfig.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-04 | **1.7.19** | **Feat pipeline_updater: Accion add_task para insertar tasks en stages existentes + Fix search_variables**: (1) **Nueva accion `add_task`**: `_process_add_task_actions()` en `update_engine.py` permite insertar una task nueva dentro de stages existentes, antes (`before_task`) o despues (`after_task`) de una task existente identificada por `displayName`. Soporta filtro por `stage` (opcional, default: todos los stages) y comodines fnmatch en el nombre de la task de referencia. (2) **Template**: `pipe_cd_insert_task_before_show_manifest.yaml` inserta task script bash que consolida bloques `env:` y remueve anotaciones `admission.datadoghq.com/enabled` del manifiesto k8s antes de la task "show Manifest". (3) **Fix search_variables**: `search_engine.py` ahora normaliza items de `var_names` (dicts con `name` o strings) antes de pasarlos a `_matches_pattern()`, igual que `search_stages()`. (4) **Tests**: 9 tests unitarios en `test_add_task.py` cubriendo: insercion before/after, filtro por stage, task inexistente, errores de validacion, comodines, preservacion de contenido. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/search_engine.py`, `scm/azdo/pipeline_updater/test_add_task.py`, `scm/templates/pipe_cd_insert_task_before_show_manifest.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-08-03 | **1.7.18** | **Feat cd_detailed_inventory: Extraccion de variables + filtro por --var-name/--var-value**: Mejora de la herramienta `cicd_inventory_cd_detailed.py` (Tool 15). (1) **Extraccion de variables**: `_fetch_cd_pipeline` ahora hace fetch del full definition detail (GET /definitions/{id}) que incluye el campo `variables`. Se extraen todas las variables con nombre y valor. (2) **Columnas nuevas en reporte**: `variableCount`, `variableNames`, `variables` (string legible `name=value; ...`), y columnas individuales `var_<name>` por cada variable encontrada (ej: `var_branchConfig`). (3) **Filtro por variable**: Nuevos args `--var-name` y `--var-value` permiten filtrar pipelines que tienen una variable especifica (ej: `--var-name branchConfig`) o con un valor concreto (ej: `--var-name branchConfig --var-value cadenaSuministro`). (4) **Fix sintaxis**: Corregido bug pre-existente en bloques try/except de imports (rich y export_manager estaban anidados incorrectamente). (5) **tools.py**: Opcion 15 actualizada con nuevos args y descripcion. (6) **Tests**: 15 tests unitarios en `test_cd_detailed_inventory.py` cubriendo: extraccion de variables (5), conversion a string (3), integracion en _fetch_cd_pipeline (3), filtrado por nombre/valor (4). | `scm/azdo/cicd_inventory_cd_detailed.py`, `scm/azdo/test_cd_detailed_inventory.py`, `scm/azdo/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-31 | **1.7.17** | **Feat release_cd_health: Columna isDisabled + HTML Dashboard interactivo**: Mejora de la herramienta `azdo_release_cd_health.py` (Tool 3). (1) **Columna `isDisabled`**: Agregada a todos los reportes de salida (Excel, CSV, JSON) y a la salida en terminal (Rich table y fallback print). La columna indica si un pipeline esta deshabilitado (`isDisabled=true` en Azure DevOps). (2) **Resumen Rich**: Se agrega conteo de pipelines Disabled vs Active en el panel de resumen. (3) **HTML Dashboard obligatorio**: Nueva funcion `generate_html_dashboard()` que genera un archivo HTML interactivo con dark theme, metric cards (total, score promedio, disabled, nunca desplegado), Chart.js doughnut (distribucion de score) + bar (consistencia de stages), tabla sortable con columna Disabled, barra de filtros (busqueda, filtro por disabled, filtro por rating). El HTML se genera siempre al finalizar el analisis, independientemente del `--output` seleccionado. (4) **`--output html`**: Agregada opcion `html` al argumento `--output` para exportar explicitamente solo HTML. (5) **Tests**: 12 tests unitarios en `test_release_cd_health.py` cubriendo: is_disabled en process_pipeline (3), is_disabled en export flat data (1), HTML dashboard (8: generacion, columna disabled, proyecto, Chart.js, conteo disabled, tabla sortable, filtros, rows vacio). | `scm/azdo/azdo_release_cd_health.py`, `scm/azdo/test_release_cd_health.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-31 | **1.7.16** | **Fix pipeline_updater: Disable usa PUT isDisabled=true, no DELETE**: El flujo de disable usaba DELETE (soft-delete, isDeleted=true) lo cual eliminaba el pipeline de la UI. Corregido para usar PUT con `isDisabled=true`, que es el comportamiento real del UI de Azure DevOps: el pipeline permanece visible pero no permite crear releases. (1) **`azdo_client.py`**: `update_release_definition` ahora acepta parametro `disable=True` que setea `isDisabled=true` en el body y no lo remueve de `fields_to_remove`. (2) **`parallel_executor.py`**: Flujo disable ahora llama `update_release_definition(disable=True)` en vez de `delete_release_definition`. (3) **`pipe_cd_disable_pipeline.yaml`**: Descripcion actualizada a v1.1. (4) **Tests**: 15 tests en `test_disable_pipeline.py` (5 nuevos para PUT con disable, 5 para executor con PUT). Suite completa: 58 tests en verde. | `scm/azdo/pipeline_updater/azdo_client.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/test_disable_pipeline.py`, `scm/templates/pipe_cd_disable_pipeline.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-31 | **1.7.15** | **Feat pipeline_updater: Deshabilitar pipelines CD via template**: Nueva funcionalidad para soft-delete de pipelines CD mediante `update.pipeline.action: "disable"` en templates YAML. (1) **`azdo_client.py`**: Nuevo metodo `delete_release_definition()` que llama DELETE API de Azure DevOps (soft-delete, recuperable). (2) **`template_parser.py`**: Nuevo metodo `get_pipeline_action()` que parsea `update.pipeline.action` del template. (3) **`parallel_executor.py`**: Cuando `pipeline_action='disable'`, ramifica al flujo de delete (download → snapshot → search → DELETE) en lugar del flujo normal (download → snapshot → search → update → PUT). (4) **`validator.py`**: Acepta `update.pipeline` como regla valida. (5) **Template**: `pipe_cd_disable_pipeline.yaml` con busqueda por stage "Production". (6) **Tests**: 14 tests nuevos en `test_disable_pipeline.py` cubriendo parser, validator, client y executor. Suite completa: 57 tests en verde. | `scm/azdo/pipeline_updater/azdo_client.py`, `scm/azdo/pipeline_updater/template_parser.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/validator.py`, `scm/azdo/pipeline_updater/test_disable_pipeline.py`, `scm/templates/pipe_cd_disable_pipeline.yaml`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-31 | **1.7.14** | **Fix pipeline_updater: Rename stage skip-safe para multiples variantes de pipelines**: `_rename_stage` lanzaba `ValueError` cuando el `source_stage` no existia o cuando `new_name` ya existia, abortando todas las reglas subsiguientes (reorder, otras renames). Ahora skip silencioso en ambos casos: (1) si source_stage no existe (ej: template tiene "Cedis Texcoco" pero el pipeline tiene "Texcoco"), (2) si new_name ya existe como stage distinto (ej: pipeline ya tiene "Production"). Esto permite que un template cubra multiples variantes de pipelines sin abortar. 3 tests de regresion agregados/actualizados. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/test_copy_stage.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-31 | **1.7.13** | **Fix pipeline_updater: Reorder stages renumera todos los ranks consecutivamente**: `_reorder_stages` solo asignaba ranks a los stages listados en el template, dejando los demás con su rank original. Esto causaba ranks duplicados o no consecutivos, generando error `VS402874: The ranks of release pipeline stages need to be consecutive natural numbers starting from the number '1'`. Ahora se renumeran TODOS los stages consecutivamente desde 1 despues de aplicar los ranks explicitos del template. 4 tests de regresion agregados. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/test_copy_stage.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-30 | **1.7.12** | **Fix pipeline_updater: Rename stage ahora actualiza referencias de dependencia**: `_rename_stage` buscaba dependencias en `deployPhases[].deploymentInput.conditions` (campo incorrecto) y bloqueaba el renombrado con error. Azure DevOps almacena dependencias en `environment.conditions[]` (`conditionType=environmentState`, `name=stage`) y `deployPhases[].deploymentInput.condition` (string `succeeded('StageName')`). Ahora actualiza ambas referencias al nuevo nombre. Fix adicional: `azdo/tools.py` maneja rutas duplicadas de template YAML (autocompletado del shell). 3 tests de regresion agregados. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/test_copy_stage.py`, `scm/azdo/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-30 | **1.7.11** | **Feat gcp_monitor: Parametro --multi-project para multiples proyectos**: Se agrega el parametro `--multi-project` a `gcp_monitor.py` para recibir multiples proyectos separados por coma de forma explicita. `--project` queda para un solo proyecto (backward compatible). En `tools.py` se agrega el conjunto `MULTI_PROJECT_PARAM_SCRIPTS` para distinguir scripts que usan `--multi-project` de los que usan `--project` con comma-separated. `run_tool` y `run_all_checkers` usan `--multi-project` cuando hay multiples proyectos y el script lo soporta. | `scm/gcp/monitoring/gcp_monitor.py`, `scm/gcp/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-30 | **1.7.10** | **Fix tools.py: Soporte dual de multiples proyectos por script**: Se anade el conjunto `MULTI_PROJECT_SCRIPTS` con los scripts que soportan comma-separated en `--project` (gcp_gateway_checker, gcp_load_balancer_checker, gcp_monitor, gcp_service_account_checker, gcp_sa_multi_project_reporter). Si el script esta en el conjunto, se pasan todos los proyectos como `--project proj1,proj2,proj3` en una sola invocacion (acumulado). Si no esta en el conjunto, se ejecuta una vez por proyecto en un loop (uno a uno). Aplicado tanto a `run_tool` como a `run_all_checkers`. | `scm/gcp/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-30 | **1.7.9** | **Fix tools.py: Restaurar paso de multiples proyectos separados por coma a --project**: El commit 19feb96 habia cambiado el comportamiento de tools.py para ejecutar scripts una vez por proyecto en un loop, en lugar de pasar todos los proyectos como string separado por coma al flag `--project`. Esto impedia que los scripts que soportan multiples proyectos (gcp_gateway_checker, gcp_load_balancer_checker, gcp_monitor) acumularan resultados. Restaurado el comportamiento anterior: se pasa `--project proj1,proj2,proj3` en una sola invocacion, tanto en `run_tool` como en `run_all_checkers`. | `scm/gcp/tools.py`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-29 | **1.7.8** | **Gateway Services Checker v2.4.0 - HTTPRoute Duplicate Detection mejorado**: (1) **Eliminado nivel MEDIUM**: El nivel MEDIUM generaba combinaciones O(n²) de todas las routes en mismo gateway+hostname sin sectionName, produciendo una mega-tabla de falsos positivos. Ahora solo se reportan conflictos concretos: CRITICAL (path+method+headers+queryParams exactos) y HIGH (PathPrefix solapados). (2) **DUPLICATES en view all**: Corregido bug donde la seccion DUPLICATES no se ejecutaba con `--view all` (default), solo con `--view duplicates`. (3) **Orden de secciones**: GATEWAYS > POLICIES > ROUTES > DUPLICATES > SERVICES. (4) **Tabla ROUTES ordenada por PathPrefix descendente**: Paths similares quedan visualmente adyacentes para detectar solapamientos manualmente. (5) **Dashboard actualizado**: Card de Duplicates sin MEDIUM, JS y Python severity maps sin MEDIUM. (6) **Limpieza completa**: Eliminadas todas las referencias a MEDIUM de codigo, tests, dashboard y README. (7) **Tests**: 68 tests pasan (46 route_duplicates + 22 dashboard). | `scm/gcp/gateway-services/gcp_gateway_checker.py`, `scm/gcp/gateway-services/dashboard_generator.py`, `scm/gcp/gateway-services/test_route_duplicates.py`, `scm/gcp/gateway-services/test_dashboard_generator.py`, `scm/gcp/gateway-services/README.md`, `VERSION`, `README.md`, `README.version.md` |
| 2026-07-29 | **1.7.7** | **Fix HTTPRoute duplicate detection: PathPrefix matching + headers/queryParams differentiation**: La deteccion de duplicados ignoraba headers y queryParams, causando falsos positivos. (1) **`_extract_route_keys`**: Ahora extrae `headers` y `query_params` de cada match usando `_normalize_headers()` y `_normalize_query_params()` (frozenset orden-independiente). (2) **`_paths_overlap`**: Corregido PathPrefix matching para usar segmentos de path (`p1 + '/'`) en lugar de `startswith` plano, evitando falsos positivos como `/api` vs `/api-v1`. Agregado `_normalize_path()` para trailing slash. Soporte de root `/` como prefix universal. (3) **`detect_route_duplicates`**: CRITICAL ahora requiere mismos headers+queryParams. HIGH skip si headers o queryParams difieren. MEDIUM agrupa por (gateway, hostname, headers, queryParams) para no flagear routes diferenciadas. (4) **`print_duplicates_table`**: Agregadas columnas Headers y Query Params en tabla de conflictos. (5) **Tests**: 66 tests total (46 route_duplicates + 20 dashboard), +28 tests nuevos cubriendo: PathPrefix segment matching, root path, trailing slash, non-segment false positives, header/queryParam differentiation, normalization helpers. | `scm/gcp/gateway-services/gcp_gateway_checker.py`, `scm/gcp/gateway-services/test_route_duplicates.py`, `VERSION`, `README.version.md` |
| 2026-07-29 | **1.7.6** | **Fix output_dir: Usar get_output_dir() en 13 checker scripts GCP**: Los scripts importaban `get_output_dir()` pero ignoraban la funcion, hardcodeando `outcome/` relativo al script. Ahora usan `get_output_dir()` que respeta `DEVSECOPS_OUTPUT_DIR` del `config.json` global. (1) **Scripts corregidos (13)**: gcp_gateway_checker.py, gcp_load_balancer_checker.py, gcp_ip_addresses_checker.py, gcp_vpc_networks_checker.py, gcp_secrets_configmaps_checker.py, gcp_cluster_checker.py, pod_connectivity_checker.py, gcp_database_checker.py, gcp_sql_comparator.py, gcp_disk_checker.py, deployment_validator.py, gcp_certificate_checker.py, gcp_cloud_armor_checker.py (2 funciones). (2) **Load Balancer Checker v1.2.0**: Soporte para multiples proyectos separados por coma en `--project`. `check_gcp_connection` verifica cada proyecto individualmente. `main()` itera sobre proyectos y mergea datos. Sanitizacion de nombre de archivo para multiples proyectos (`multi_N_projects`). (3) **Tests**: 11 tests load-balancer + 20 tests dashboard, 31 total pasan. | `scm/gcp/gateway-services/gcp_gateway_checker.py`, `scm/gcp/load-balancer/gcp_load_balancer_checker.py`, `scm/gcp/vpc-networks/gcp_ip_addresses_checker.py`, `scm/gcp/vpc-networks/gcp_vpc_networks_checker.py`, `scm/gcp/secrets-configmaps/gcp_secrets_configmaps_checker.py`, `scm/gcp/cluster-gke/gcp_cluster_checker.py`, `scm/gcp/connectivity/pod_connectivity_checker.py`, `scm/gcp/cloud-sql/gcp_database_checker.py`, `scm/gcp/cloud-sql/gcp_sql_comparator.py`, `scm/gcp/cloud-sql/gcp_disk_checker.py`, `scm/gcp/connectivity/deployment_validator.py`, `scm/gcp/certificate-manager/gcp_certificate_checker.py`, `scm/gcp/cloud-armor/gcp_cloud_armor_checker.py`, `VERSION`, `README.version.md` |
| 2026-07-24 | **1.7.5** | **Pipeline Updater - Acciones copy y add para Stages**: Implementación completa para insertar nuevos stages en pipelines CD. (1) **action: copy**: clona un stage existente (`source_stage`) y lo inserta con un nuevo nombre (`new_name`). Soporta modificación de atributos de tasks dentro del stage copiado (`task_updates`). (2) **action: add**: inserta un stage nuevo desde una definición embebida (`definition`). (3) **Posiciones de inserción**: `after`, `before`, `between`, `start`, `end`. `between` requiere `after_stage` y `before_stage`. (4) **Ranks automáticos**: al insertar stages, los `rank` se reasignan secuencialmente (1, 2, 3...) según el orden del array, ya que Azure DevOps ordena por `rank`. (5) **IDs automáticos**: los IDs de nuevos stages se asignan como max existente + 1. (6) **Pruebas**: `test_copy_stage.py` con 10 tests (copy, add, task_updates, between, validaciones). Suite del módulo 30 tests en verde. (7) **Documentación**: README del módulo actualizado con ejemplos de copy/add. (8) **Template**: `pipe_cd_copy_stage.yaml` como ejemplo de uso. | `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/test_copy_stage.py`, `scm/azdo/pipeline_updater/README.md`, `scm/templates/pipe_cd_copy_stage.yaml`, `VERSION`, `README.version.md` |
| 2026-07-24 | **1.7.4** | **Pipeline Updater - Comentario de Revisión desde Template**: El campo `metadata.comment` del template ahora se usa como comentario de la revisión del release en Azure DevOps, visible en el historial de la definición. (1) **Modelo**: se agregó `comment` a `TemplateMetadata`. (2) **Parser**: `get_metadata()` lee `metadata.comment`. (3) **Cliente**: `update_release_definition()` acepta el parámetro opcional `comment` y lo asigna a `definition['comment']`; si no se provee, no altera el comentario existente. (4) **Ejecutor**: `_process_pipeline()` pasa el comentario del template al `PUT`. (5) **Pruebas**: `test_comment_sent_when_provided` y `test_comment_omitted_when_none`; suite del módulo 20 tests en verde. (6) **Validado** contra el pipeline `2016` (revision 27, comentario persistido correctamente). | `scm/azdo/pipeline_updater/models.py`, `scm/azdo/pipeline_updater/template_parser.py`, `scm/azdo/pipeline_updater/azdo_client.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/test_update_release_definition.py`, `scm/azdo/pipeline_updater/README.md`, `VERSION`, `README.version.md` |
| 2026-07-24 | **1.7.3** | **Pipeline Updater - Fix de Persistencia y Reordenamiento de Stages**: Correcciones al Pipeline Updater (Tool 41) para que el reordenamiento de stages se persista correctamente en Azure DevOps. (1) **Fix de revisión (HTTP 400)**: Se dejó de incrementar `revision` manualmente; Azure DevOps usa `revision` para concurrencia optimista y requiere la misma revisión descargada (el servidor la incrementa). Esto resolvía el error "You are using an old copy of the release pipeline". (2) **Fix de reordenamiento**: `_reorder_stages()` ahora asigna el campo `rank` a cada environment. En Azure DevOps el orden lo determina `rank`, no la posición en el array; antes solo se reordenaba el array y el cambio no se reflejaba. (3) **Limpieza de campos de solo lectura**: se agregó `lastRelease` a la lista removida antes del `PUT`; uso de `deepcopy` para no mutar la definición original; el cuerpo del error de la API se incluye en la excepción. (4) **Pruebas de unidad**: `test_update_release_definition.py` (5) y `test_reorder_stages.py` (4). (5) **Validado** contra el pipeline `2016` (orden Develop→QA→Staging→Production, revision 26). | `scm/azdo/pipeline_updater/azdo_client.py`, `scm/azdo/pipeline_updater/update_engine.py`, `scm/azdo/pipeline_updater/parallel_executor.py`, `scm/azdo/pipeline_updater/README.md`, `scm/azdo/pipeline_updater/test_*.py`, `VERSION`, `README.version.md` |
| 2026-07-18 | **1.7.2** | **GCP Monitor Fase 2 - Métricas de Uso Actual**: Implementación completa de integración Cloud Monitoring API para obtener métricas de uso en tiempo real. (1) **Módulo gcp_monitoring_metrics.py**: Nuevas funciones `get_gke_usage_metrics()` y `get_compute_usage_metrics()` que obtienen CPU/Memoria usado (%) para GKE y CPU/Memoria/Disco usado (%) para Compute Engine. Procesamiento paralelo con ThreadPoolExecutor. Manejo robusto de errores y fallbacks. (2) **Integración en gcp_monitor.py**: Funciones de métricas integradas en `create_consolidated_detailed_tables()`. Obtención paralela de métricas para todos los clusters e instancias. Formatos de salida consistentes. (3) **Tablas Actualizadas**: GKE ahora muestra CPU Usado (%) y Memoria Usada (%). Compute Engine ahora muestra CPU Usado (%), Memoria Usada (%), Disco Usado (%). Todos los valores con formato "X.X%" o "N/A". (4) **Requisitos**: Cloud Monitoring API habilitada. Google Cloud Ops Agent instalado en Compute Engine (para memoria y disco). (5) **Fallbacks**: Si Monitoring API no disponible, muestra "N/A". Si métrica no disponible, muestra "N/A". Logging completo de errores. Commits: 2440d79. | `scm/gcp/monitoring/gcp_monitoring_metrics.py`, `scm/gcp/monitoring/gcp_monitor.py`, `VERSION`, `README.version.md` |
| 2026-07-18 | **1.7.1** | **GCP Monitor Fase 1 - Capacidad + Paralelismo + Validación + JSON**: Implementación completa de métricas de capacidad, procesamiento paralelo, validación exhaustiva y exportación JSON estándar. (1) **Diccionario de Mapeo**: MACHINE_SPECS con 60+ tipos de máquinas. (2) **Tablas Mejoradas**: GKE (CPU Total, Memoria Total, CPU Usado %, Memoria Usada %), Compute Engine (CPUs, Memoria, Disco Raíz, CPU Usado %, Memoria Usada %, Disco Usado %). (3) **Procesamiento Paralelo**: Dos niveles - Nivel 1: 6 recursos por proyecto en paralelo. Nivel 2: Múltiples proyectos en paralelo. Mejora: 3-5x más rápido. (4) **Validación Exhaustiva**: VALIDATION_CALCULOS.md con validación de fórmulas, origen de datos GCP, edge cases. (5) **Exportación JSON Estándar**: export_to_json mejorada para consolidado o individual. Estructura: report_metadata (versión, timestamps, zona horaria), summary (conteos), data (recursos por proyecto). Archivos: gcp_report_consolidated_YYYYMMDD_HHMMSS.json o gcp_report_{project_id}_YYYYMMDD_HHMMSS.json. (6) **Documentación**: PARALLEL_PROCESSING.md, JSON_EXPORT_STANDARD.md. Commits: 39558f2, 12d0c0e, c044930, 6472b9b, 7e25c51, 63d3fa3, b167e1a, 52ceee7, 4be5514. | `scm/gcp/monitoring/gcp_monitor.py`, `scm/gcp/monitoring/CAPACITY_ANALYSIS.md`, `scm/gcp/monitoring/VALIDATION_CALCULOS.md`, `scm/gcp/monitoring/PARALLEL_PROCESSING.md`, `scm/gcp/monitoring/JSON_EXPORT_STANDARD.md` |
| 2026-07-15 | **1.7.0** | **Ejecutables Compilados + Documentación de Templates + Análisis de Pendientes**: (1) **Ejecutables Compilados**: Sistema completo de compilación con PyInstaller para Windows (toolbox.exe) y Linux (toolbox). Scripts wrapper (toolbox.bat, toolbox.sh) para facilitar uso. Ejecutables standalone sin dependencias externas (~50-80 MB). (2) **Documentación de Templates**: Sección completa en README.md sobre 6 templates YAML para Pipeline Updater (Docker, Kubernetes, Variables, Azure, Scripts, Migración). Ejemplos YAML completos, casos de uso, guía paso a paso, estructura detallada, checklist de seguridad. (3) **Análisis de Pendientes**: Análisis exhaustivo de 83 documentos en docs/ identificando 4 proyectos principales (Dashboard Matutino, Cloud Run Features, Pipeline Health, Features Adicionales). Documentos generados: ANALISIS_PENDIENTES_IMPLEMENTACION.md (533 líneas), RESUMEN_EJECUTIVO_PENDIENTES.md (374 líneas). (4) **Compilación Automática**: Script build_executables.py (180+ líneas) con detección de SO, validación de PyInstaller, compilación automática, creación de wrappers. (5) **Documentación Completa**: BUILD_INSTRUCTIONS.md (350+ líneas), dist/README.md (200+ líneas), actualización de README.md (+78 líneas). Total: 800+ líneas de documentación nueva. (6) **Commits**: 4848bf6 (Templates), 0d36b65 (Resumen ejecutivo), 0e813b8 (Análisis pendientes), 86876c6 (Ejecutables compilados). | `scm/main.py`, `build_executables.py`, `toolbox.bat`, `toolbox.sh`, `BUILD_INSTRUCTIONS.md`, `dist/README.md`, `README.md`, `docs/ANALISIS_PENDIENTES_IMPLEMENTACION.md`, `docs/RESUMEN_EJECUTIVO_PENDIENTES.md`, `VERSION` |
| 2026-07-14 | **1.7.0p** | **Azure Cloud Platform Integration - 25 Herramientas SRE**: Replicación completa de estructura GCP a Azure con 25 herramientas 100% funcionales. (1) **Estructura Azure** (`scm/azure/`): 14 subdirectorios (app-service, azure-sql, cluster-aks, connectivity, consolidation, event-tracker, inventory, monitoring, notification, reports-viewer, rolesypermisos, scripts-console, secrets-configmaps, service-accounts). (2) **25 Herramientas**: Monitoreo (2), IAM/Security (3), Database (3), Networking (4), Kubernetes AKS (6), App Service (3), Inventory (1), Reports (1), Event Tracker (1), Dashboard (1). Todas con status="ready". (3) **Integración main.py**: Azure como opción 2, con validación de subscription_id, variables de entorno (AZURE_SUBSCRIPTION_ID, AZURE_TENANT_ID, AKS_CLUSTER_NAME, etc.), y mapeo de plataforma. (4) **Autenticación Automática**: Función `validate_platform_authentication()` que valida `az account show` y ejecuta `az login` si es necesario. Configurable en `config.json` con flags `auth.login.azure.enabled` y `auto_login`. (5) **Configuración**: Sección `azure` en `config.json.template` con subscription_id, tenant_id, region, credentials (cli/service_principal), kubernetes config. (6) **Documentación**: README.md completo para módulo Azure con 25 herramientas, instalación, configuración, autenticación, ejemplos, troubleshooting. (7) **Nombres Azure Específicos**: AKS (no GKE), App Service (no Cloud Run), ACR (no Artifact Registry), Azure SQL (no Cloud SQL), VNets/NSGs (no VPC), RBAC (no IAM). (8) **Commits**: 54c67c1 (Soporte Azure), 36227da (Status ready), 61504f6 (README Azure), 677004e (README principal). | `scm/azure/`, `scm/main.py`, `scm/config.json.template`, `README.md`, `VERSION` |
| 2026-07-13 | **1.6.21** | **Pipeline Updater Template (Tool 41) - Actualización Masiva de Pipelines CD**: Implementación completa de herramienta para actualización masiva de pipelines CD usando templates YAML. (1) **Módulo pipeline_updater** (~2,600 líneas): 10 clases (TemplateParser, TemplateValidator, AzureDevOpsClient, SearchEngine, UpdateEngine, ParallelExecutor, Reporter, PipelineUpdater) + CLI. (2) **Características**: Búsqueda inteligente (stages, tasks, variables, artifacts), actualización segura con snapshots automáticos, ejecución paralela (5 workers), reportería (JSON/CSV/HTML), rollback automático. (3) **Integración tools.py**: Herramienta 41 con menú interactivo (3 opciones): [1] Actualizar pipelines, [2] Rollback desde snapshot, [3] Listar snapshots. (4) **Validaciones**: Definition IDs, template path, parámetros comunes (org, project, pat), workers, dry-run. (5) **Documentación**: README.md completo con ejemplos, especificación de template YAML, casos de uso. (6) **Release**: Tag 1.6.21, archivo devsecops-toolbox-1.6.21.tar.gz (6.48 MB), publicado en GitHub. | `scm/azdo/pipeline_updater/`, `scm/azdo/tools.py`, `README.md`, `VERSION` |
| 2026-07-13 | **1.6.20** | **Testing & Coverage Expansion**: Expansión significativa de cobertura de tests con 123 tests nuevos, alcanzando 370 tests totales. (1) **Tests por Plataforma**: 16 tests AZDO (PR Checker, Branch Policies, Release Health, Pipeline Drift, Task Validator, Pipeline Updater, Rollback, Redo), 18 tests GCP (Service Accounts, Cloud SQL, GKE, Cloud Run, Connectivity, Cloud Functions), 18 tests AWS (IAM, RDS, VPC, EKS, ECR, Lambda, S3), 30 tests KPI (DORA, SRE, Security, Cost, Quality, Performance, Maturity Levels, Benchmarks, Scoring). (2) **Consolidación de Tests**: Todos los tests movidos a `scm/tests/unit/` para mejor mantenibilidad. Removidos tests con imports incompatibles. (3) **Tests Adicionales**: test_analyzer_kpi.py (13), test_reporter_kpi.py (13), test_output_manager.py (15), test_pipeline_rollback_redo.py (24). (4) **Estadísticas**: 370 tests pasando, 0 fallos, 12.79% cobertura, 3.88s ejecución. | `scm/tests/unit/`, `RELEASE_NOTES_1.6.20.md` |
| 2026-07-13 | **1.6.19** | **Pipeline Redo & Logging System**: (1) **Opción Redo** (Tool 22, Opción 6): Nueva funcionalidad para volver a la versión previa de un pipeline basado en definition_id. Obtiene revisión anterior, muestra información detallada, solicita confirmación (requiere escribir "SI"), soporta modo DRY-RUN. (2) **Logging Completo**: Todas las operaciones de Redo registran en `outcome/redo_pipeline_[id]_[timestamp].log` con timestamps, niveles (INFO/WARNING), y eventos clave (inicio, obtención de definiciones, confirmación, ejecución, fin). (3) **Dockerfile Enhancement**: Agregado `nano` para editar/copiar templates dentro del contenedor. (4) **Documentación**: Actualizado README.md con nueva funcionalidad de Redo. (5) **Validación de Templates**: Sistema preparado para leer y validar archivos template antes de ejecutar operaciones. | `scm/azdo/pipeline-cd-rollback-pipeline.py`, `scm/azdo/tools.py`, `Dockerfile`, `README.md` |
| 2026-07-09 | **1.6.18** | **KPI Analyzer Pro v1.9.6 - Integración Dashboard Completa**: Integración exitosa de Dashboard Matutino en KPI Analyzer Pro. (1) **5 Módulos Nuevos** (~1,980 líneas): health_score.py (DORA metrics), exporter.py (JSON/CSV/HTML/Excel), consolidator.py (multi-fuente), generator.py (dashboards HTML), scheduler.py (planificación automática). (2) **16 Herramientas Integradas**: 5 análisis KPI + 7 dashboards/consolidación + 4 exportación. (3) **Correcciones**: Fix acceso defensivo a 'emoji' y 'name' en tools.py con .get(). (4) **Limpieza**: Opción 6 (Dashboard) removida de main.py, consolidada en Opción 5. (5) **Configuración**: config.json.template actualizado a v1.9.6 con Dashboard integrado. | `scm/kpi_analyzer/`, `scm/main.py`, `scm/config.json.template` |
| 2026-07-08 | **1.6.17** | **Validación y Limpieza de Configuración**: Actualización de versión en config.json.template a 1.6.17 con referencias a Dashboard integrado en KPI Analyzer Pro. | `scm/config.json.template` |
| 2026-07-08 | **1.6.16** | **Tool 4 & Tool 38 Enhancements - Múltiples Proyectos GCP**: (1) **Tool 4 (Service Account Checker)** - Soporte para múltiples proyectos GCP con carga automática desde `config.json`. Permite override con `--projects=proj1,proj2,proj3`. Procesamiento paralelo con 5 workers. Tabla de resultados por proyecto. Spinner animado y barra de progreso. (2) **Tool 38 (Service Accounts Multi-Project Reporter)** - Visualización profesional con Rich library. Spinners y progreso durante extracción. Tabla de extracción por proyecto con columna de proyecto. Tabla de resumen final con duración. Procesamiento paralelo (5 workers). Fallback a print() si Rich no disponible. (3) **Integración tools.py** - Tool 4 no pregunta por proyecto, permite carga automática desde config.json. Cambio: `"args": ["--project", "-o"]` → `"args": ["-o"]`. Commits: `fd4d71e`, `c64033f`, `f3e68a5`, `e19a373`. | `scm/gcp/service-account/gcp_service_account_checker.py`, `scm/gcp/service-accounts/gcp_sa_multi_project_reporter.py`, `scm/gcp/tools.py` |
| 2026-07-07 | **1.6.15** | **GCP Infrastructure Consolidation & Cloud Functions Analysis**: Implementación completa de 3 nuevas herramientas profesionales para consolidación de infraestructura GCP (Tools 35, 36, 37). (1) **Tool 35: Cloud Functions Analyzer** - Análisis profundo de Cloud Functions con seguridad, costos, triggers y performance. (2) **Tool 36: Infrastructure Consolidator** - Consolidación de Load Balancers, Cloud Run y Cloud Functions con mapeo de relaciones e identificación de servicios huérfanos. (3) **Tool 37: Unified Infrastructure Dashboard** - Dashboard ejecutivo unificado con alertas automáticas y recomendaciones. Incluye: 9 archivos de código (~3,200 líneas), 36 tests unitarios (100% exitosos), 6 documentos de análisis y guías (~2,700 líneas). Correcciones: manejo de diccionarios en tablas Rich, validación mejorada de sesión gcloud. Organización: archivos .md agrupados en carpetas temáticas (architecture, planning, analysis, sessions, corrections). Documentación de estructura agregada. | `scm/gcp/cloud-functions/`, `scm/gcp/consolidation/`, `docs/feature_loadbalancer/`, `tests/`, `docs/` |
| 2026-06-29 | **1.6.12** | **Dinamización de Menús - Eliminar Hardcode**: Implementación completa de generación dinámica de opciones de sistema (A, B, Q) en todas las plataformas. (1) **Estructura _system_options**: Diccionario de configuración con tipo, nombre, descripción y exclusiones. (2) **Función get_auto_tools()**: Genera lista de herramientas dinámicamente iterando por GROUP_ORDER y excluyendo especificadas. (3) **Función build_system_options()**: Construye opciones finales desde _system_options. (4) **Inicialización automática**: _init_system_options() al cargar módulo. Reducción: 113 → 56 líneas (50%), mapeos duplicados: 6 → 1 (83%), puntos de cambio: centralizados. Implementado en: azdo/tools.py, gcp/tools.py, aws/tools.py, terminal/tools.py, kpi_analyzer/tools.py. Sin cambios en API pública, comportamiento o salidas. Totalmente retrocompatible. | `scm/azdo/tools.py`, `scm/gcp/tools.py`, `scm/aws/tools.py`, `scm/terminal/tools.py`, `scm/kpi_analyzer/tools.py` |
| 2026-06-29 | **1.6.11** | **Análisis de Refactorización de Menús y Búsqueda Interactiva**: (1) Análisis exhaustivo de hardcode en menús: 113 líneas de hardcode identificadas en 6 archivos (main.py, azdo/tools.py, gcp/tools.py, aws/tools.py, terminal/tools.py, kpi_analyzer/tools.py). Propuesta de solución: generación dinámica de opciones de sistema con estructura `_system_options` y funciones reutilizables `get_auto_tools()` y `build_system_options()`. Reducción estimada: 50% de código (113 → 56 líneas). (2) Análisis de búsqueda interactiva: módulo `interactive_search.py` implementado en AZDO (328 líneas) con búsqueda fuzzy, captura de teclas multiplataforma (Windows/Linux/macOS), visualización con Rich. Problema: solo en AZDO (17% cobertura). Propuesta: crear módulo centralizado `scm/search_module.py` para expandir a todas las plataformas (100% cobertura). (3) Documentación completa en `docs/refactor_arquitectura/`: `ANALISIS_DINAMIZACION_MENUS.md`, `ANALISIS_COMPLETO_HARDCODE_MENUS.md`, `ANALISIS_BUSQUEDA_INTERACTIVA.md`. | `docs/refactor_arquitectura/`, `scm/azdo/interactive_search.py`, `scm/main.py`, `scm/*/tools.py` |
| 2026-06-18 | **1.6.10** | **Azure DevOps Pipeline Updater & Rollback System**: (1) **Pipeline Updater** (Tool 21, v1.0.6): Actualización masiva de pipelines (hasta 100) con batch processing, backup automático antes de cada cambio, confirmación con resumen de cambios, reportes JSON detallados, modo dry-run, y comentarios automáticos en pipelines. (2) **Pipeline Rollback** (Tool 22, v1.2.0): Sistema completo de rollback con 3 métodos - Full Backup Restore (máxima seguridad), Hybrid Rollback (usa revisión del backup desde Azure DevOps, lo mejor de ambos mundos), y Manual Revision (máxima flexibilidad). Incluye listado de backups/revisiones, validación de estructura, confirmaciones obligatorias, y modo dry-run. Backups almacenados en `outcome/backups/` con metadata completa. **AZDO Tools** actualizado a v1.3.4 con integración de ambas herramientas. | `scm/azdo/update-pipeline-cd-branchconfig.py`, `scm/azdo/rollback-pipeline.py`, `scm/azdo/tools.py` |
| 2026-06-09 | **1.6.9p** | **KPI Analyzer Fixes**: Correcciones críticas para KPI Analyzer: (1) Fix `MissingStyle` error eliminando `.replace('#', '')` en `border_style` (Rich requiere `#` en colores hex), (2) Fix `NameError` agregando import y inicialización de `DashboardGenerator`, (3) Agregadas dependencias faltantes a `requirements.txt` (pyyaml, pandas, plotly, streamlit), (4) Creados 17 unit tests en `test_kpi_analyzer.py` para mejorar cobertura. **AZDO Tools Fix**: Normalización de URL de organización en `tools.py` para prevenir duplicación de prefijo `https://dev.azure.com/` que causaba errores 400 Bad Request en todos los scripts AZDO. | `scm/kpi_analyzer/analyze_kpis.py`, `scm/requirements.txt`, `scm/tests/unit/test_kpi_analyzer.py`, `scm/azdo/tools.py` |
| 2026-06-09 | **1.6.9** | **KPI Analyzer**: Sistema completo de análisis de KPIs DevSecOps con modelo de madurez de 6 niveles (0-5), 30 KPIs organizados en 6 dimensiones, benchmarks de industria (DORA, SRE, ITIL, NIST CSF, ISO 20000), análisis automático desde salidas JSON, reportes (JSON/CSV/HTML), y documentación completa. Incluye: `analyze_kpis.py`, `analyzer.py`, `reporter.py`, `maturity_model.py`, `benchmarks.py`, `kpi_schema.yaml`, `docs/kpi_sources_inventory.md`, `docs/DevSecOps_Maturity_Model.md`, `docs/KPIs_Frameworks_DevSecOps.md`. | `scm/kpi_analyzer/`, `docs/` |
| 2026-06-04 | **1.6.8** | **AWS/GCP Homologation**: `scm/aws/tools.py` alineado visualmente con `scm/gcp/tools.py` — mismos colores (`Monitoreo` cyan, `Compute` bright_blue), emojis, nombres en español, GROUP_ORDER (monitoring primero) y estructura del menú Rich (columna Grupo width=18). `scm/aws/` validado: todos los sub-paquetes válidos tienen `__init__.py`. | `scm/aws/tools.py`, `scm/aws/**/__init__.py` |
| 2026-06-04 | **1.6.7** | **Wheel Fix W004**: Resuelto `check-wheel-contents W004` (módulos en paths no importables). Añadidos `__init__.py` a 19 sub-paquetes válidos (`azdo`, `aws/*`, `gcp/connectivity|monitoring|rolesypermisos`, `terminal`). Configurado `[tool.setuptools]` con `include-package-data = false` y `namespaces = false` para excluir directorios con guiones y keyword `lambda` del wheel. | `pyproject.toml`, 19× `__init__.py` |
| 2026-06-04 | **1.6.7p** | **Coverage Fix**: `omit` en `[tool.coverage.run]` para excluir módulos AWS/GCP/Terminal sin tests. Restaura umbral `fail-under=35` (38% total, 190/190 tests). | `pyproject.toml` |
| 2026-04-19 | **1.6.5** | **Terminal Scripts**: Nueva sección en `main.py` (opción 4) con 5 scripts shell universales agnósticos de cloud. Reubicados desde `gcp/scripts-console/` a `terminal/`: Certificate TLS Report, DB Connections Checker, Deployments Last News/Update/Events. | `scm/main.py`, `scm/terminal/` |
| 2026-04-19 | **1.6.4** | **Validación de plataforma**: `tools.py` detecta ejecución en Windows y muestra diálogo informativo cuando se intenta usar herramientas exclusivas de Linux (scripts `.sh`), sugiriendo WSL/Git Bash. | `scm/**/tools.py` |
| 2026-04-19 | **1.6.3** | **Certificate TLS Report**: Nueva herramienta (23) para validar certificados SSL/TLS remotos desde GKE con valores reales de TLS version y cipher. Integrada en `tools.py` con soporte para scripts shell. | `scm/terminal/` |
| 2026-04-18 | **1.6.2** | **Cross-platform venv**: Valida que el python del venv funcione antes de usarlo; si fue creado en otra plataforma (Linux/WSL vs Windows), lo recrea automáticamente y limpia caché de requirements. | `scm/**/tools.py` |
| 2026-04-17 | **1.6.1** | **make_dist.ps1**: Solo empaqueta folder `scm/`, lee exclusiones dinámicamente desde `.gitignore` en vez de hardcodearlas. | `make_dist.ps1` |
| 2026-04-16 | **1.6.0** | **Rich UI Inventory**: Reescritura de `generar-inventario-csv.sh` a Python con Rich (spinners, barras de progreso por hilo, Panel/Tabla). Auto-instalación de rich en venv. Launcher `run_inventory.py` con importación directa (no subprocess). | `scm/gcp/inventory/` |
| 2026-04-16 | **1.5.3** | **Sync Repos**: Script `sync_repos.py` para sincronización bidireccional toolbox ↔ azdo con commit automático. Nueva herramienta Inventario GKE+Cloud SQL (opción 22). | `sync_repos.py`, `scm/gcp/inventory/` |
| 2026-04-08 | **1.5.2** | **Docker Container**: Dockerfile slim con Azure/AWS/GCP CLI, kubectl, Helm, Terraform, netshoot. Docker Compose con 3 servicios. Entrypoint script con auto-configuración. | `Dockerfile`, `docker-compose.yml` |
| 2026-04-02 | **1.5.1** | **Testing Suite**: Arquitectura profesional de testing con pytest, cobertura 70%+, mocks para GCP/AZDO/AWS, CI/CD con GitHub Actions. Tests unitarios e integración con 500+ assertions. | `scm/tests/`, `pytest.ini` |
| 2026-04-02 | **1.5.0** | **Config Unificado**: Template `config.json.template` para gestión centralizada de tokens/credenciales de AZDO, GCP y AWS. Variables de entorno automáticas al lanzar plataformas. | `config.json.template`, `scm/main.py` |
| 2026-03-31 | **1.4.1** | **AWS Toolbox**: 13 herramientas DevSecOps para AWS (IAM, RDS, VPC, EKS, ECR, EC2, Lambda, CloudWatch). | `scm/aws/` |
| 2026-03-31 | **1.1.1** | **Análisis Pro**: Reporte completo de arquitectura con 15+ mejoras priorizadas. | `ARCHITECTURE_ANALYSIS_PRO.md` |
| 2026-03-26 | **1.0.0** | **Versión inicial**: Launcher unificado para GCP y Azure DevOps. | `scm/main.py`, `scm/gcp/`, `scm/azdo/` |

---

## Versiones por Sub-proyecto

Las herramientas individuales de cada plataforma mantienen su propia versión interna en `tools.py`:

| Plataforma | `tools.py` Version | Notas |
|------------|-------------------|-------|
| **GCP** | `1.9.3` | Launcher GCP (25 herramientas) |
| **AZDO** | `1.3.4` | Launcher Azure DevOps (22 herramientas) — incluye Pipeline Updater y Rollback |
| **AWS** | `1.0.1` | Launcher AWS (19 herramientas) — homologado visualmente con GCP en v1.6.8 |
| **Terminal** | `1.0.2` | Scripts universales agnósticos de cloud (6 herramientas) |

> **Nota**: La versión del toolbox (`1.6.10`) es independiente de la versión interna de cada launcher. La versión del toolbox se usa para empaquetado (wheel), Docker tags y releases de GitHub.

---

## Convención de Versionado

El proyecto sigue [Semantic Versioning 2.0.0](https://semver.org/lang/es/):

```
MAJOR.MINOR.PATCH

MAJOR — Cambios incompatibles con versiones anteriores
MINOR — Nuevas funcionalidades (compatibles hacia atrás)
PATCH — Correcciones de bugs (compatibles hacia atrás)
```

**Regla del proyecto**: solo se generan versiones PATCH (ej: 1.6.7 → 1.6.8). No se generan versiones MAJOR ni MINOR.

---

## Referencias

- `VERSION` — Archivo fuente de verdad (versión actual del toolbox)
- `pyproject.toml` — Metadatos del paquete Python (build/wheel)
- `scm/__init__.py` — `__version__` (importable desde Python)
- Badges en `README.md` — Referencia visual al archivo `VERSION`
